# Repository Metadata

These values document the intended GitHub repository metadata for BrowseVault.

## GitHub Description

```text
Private local-first browser history search, backup, export, and preservation extension.
```

## GitHub Topics

```text
chrome-extension, browser-history, history-search, history-backup, local-first, privacy-first, export-history, manifest-v3
```

The same description and keyword set are mirrored in root `package.json` so local tooling has access to the repository positioning without reading GitHub metadata.
